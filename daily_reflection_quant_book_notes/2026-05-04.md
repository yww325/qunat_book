# 2026-05-04 读书/研究反思（Quant Book）

## 0) 今天读了什么（章节/页码/主题）

- （无）今日为 cron 自动触发的每日反思，无新增量化读书/研究记录

## 1) 今天我真正学到的3点（必须可复述/可执行）

1. **KDJ 金叉 J 值回归信号在 15m 级别触发稀疏**：历史回测（0700.HK 15m，KDJ(6,3,3）仅用同bar AND 条件几乎无触发，改用窗口 AND（T2 触发）后才获得有效样本。
2. **回撤过滤（DD >= 2%，窗口 16 bars ≈ 4h）能有效剔除强趋势延续**：带该过滤器后，强反弹率从 20%（15 triggers）提升至 40%，说明 4h 窗口内的前回撤有助于过滤已经走坏的信号。
3. **Tier 分层（Hard only vs Soft confirm）是信号质量的里要分水岭**：严格条件 Tier B（RSI6 <= 35 + Tier BHard）比含 OBV 的 Tier A 更可靠，需按目标严格度选择适用层。

## 2) 一个我之前理解错了/忽略了的点

- **之前误以为 15m 回撤应按"当日 bars"计算**：实际上 15m 数据跨日连续，回撤必须在全量连续序列上计算（跨日滚动），否则 4h 窗口会把当日 16 bars 之前的跨日夜盘数据漏掉，导致回撤被低估。正确做法是用 `rolling(16).max() - close` 在完整序列上做。

## 3) 明天要做的1步（具体动作，可在 30 分钟内完成）

- 将 quant_trading_alert_hk_15m.py 中的回撤计算改为跨日滚动版本（full 15m series rolling，窗口 16），并重新跑 Tier B + RSI6 <= 35 的回测，验证强反弹率是否进一步变化。

## 4) 【结合当前项目】可落地的2条改进建议

### 建议 A（改流程/加检查/补实验）

- **触发原因（今天学到的点）**：KDJ + RSI 组合在 15m 触发稀疏，需实验更多时间框架
- **我们项目要改什么**：在 HK 15m 策略中增加 30m 和 60m 两个时间框架的并行回测（用同一组参数），比较触发频率与质量
- **怎么验收（输出/指标）**：三个时间框架各跑 20 个交易日，输出触发次数、强反弹率、稳定率对比表

### 建议 B（改约束/加成本/加风控）

- **触发原因（今天学到的点）**：Tier B（Hard only）已有较好的信号质量，但 40% 强反弹率仍有提升空间
- **我们项目要改什么**：在 Tier B 触发后加入「距离上次 KDJ 金叉 > 20 bars」的过滤条件，减少过于密集的重复信号，同时在入场时增加固定 0.5% 止损约束
- **怎么验收（输出/指标）**：加入间距过滤后重新回测，对比原始 Tier B 的触发次数、夏普比率、最大回撤

## 5) 偏差/可交易性快速自检（打勾）

- [ ] 无前视（财务数据按可得时间 as-of 对齐）
- [ ] 无生存者偏差（包含退市/停牌长期样本的处理逻辑）
- [ ] 回测计入成本/冲击（至少保守上限）
- [x] Universe/过滤条件固定且可解释（ST/新股/停牌/涨跌停）
- [ ] 样本外/滚动验证（或至少计划了）