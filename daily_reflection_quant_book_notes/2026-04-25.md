# 2026-04-25 阅读笔记反思（由助手生成）

## 0) 今天读了什么（章节/主题/代码）

- 本周（04/21–04/24）为工作日，期间未进行量化投资书籍的系统阅读。
- 本 cron 于每周六凌晨 1:00 HKT 运行，本次记录涵盖本周整体情况。
- 当前项目主要推进：信号分层评分机制、scan_latest 优化。

## 1) 今天我真正学到的3点（可复述/可执行）

1. **cron 反思文件的积累价值**：每周末的简短复盘比零散笔记更有利于形成长期记忆，项目推进节奏也更清晰。格式模板（学到的点 + 偏差自检 + 落地建议）值得坚持。

2. **分层评分的设计思路**（延续04/20建议A）：
   - score = 0.4×(DD_prevclose/5) + 0.3×clip(28−RSI14,0,20)/20 + 0.3×clip(J_t−J_{t−3},0,15)/15
   - score≥0.6 → TierA（主动推送）；0.4–0.6 → TierB（记录不推送）；<0.4 → 忽略
   - 需配合成交额下限过滤（如成交额<500万港币则降级或过滤）

3. **Walk-forward 验证的必要性**：
   - 量化策略未经滚动窗口验证就无法判断过拟合程度
   - 前40天训练+后20天验证是最基础的门槛

## 2) 一个我之前理解错了/忽略了的点

- **忽略点**：之前认为"只要回测夏普比高就说明策略好"，但忽略了生存者偏差（survivorship bias）和前视偏差（look-ahead bias）。港股/美股停牌/退市股若不在 universe 内，会显著高估策略表现。需要在回测框架中明确记录被过滤的股票数量和原因。

## 3) 明天要做的1步（具体动作）

- 在 `backtest()` 函数中加入 `mode='walkforward'` 参数，实现最基础的滚动训练/验证框架（训练40天，验证20天，按日滚动）。

## 4) 【结合当前项目】可落地的2条改进建议

### 建议A：落地分层评分机制（优先级 P0）

- 在 `scan_latest()` 中加入 score 字段，计算使用上述三层加权公式
- TierA/TierB 分层推送逻辑 + 成交额下限过滤（建议 ≥500万港币）
- 预计改动点：`scan_latest()` → 新增 `compute_score()` → 修改推送判断逻辑

### 建议B：补 walk-forward 验证框架（优先级 P1）

- 在 `backtest()` 加 `mode='walkforward'` 参数
- 输出：滚动夏普比、最大回撤、胜率在各窗口的分布
- 配合日志记录多重检验，避免 p-hacking

## 5) 偏差/可交易性快速自检（当前项目状态）

- [ ] 无前视偏差（财务数据按 as-of 对齐）——行情数据为主，暂时安全
- [ ] 无生存者偏差（包含退市/停牌样本）——yfinance 对港股退市股覆盖不足，需补档
- [x] Universe/过滤条件明确（HK_POOL/stock_pool 在脚本里显式列出）
- [ ] 样本外/滚动验证（walk-forward 尚未实现，见建议B）
- [ ] 交易成本已纳入（目前未计入，建议分层成本惩罚：低成交额→更高滑点估计）
